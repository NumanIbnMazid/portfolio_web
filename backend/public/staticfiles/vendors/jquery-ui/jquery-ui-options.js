const jQueryUIdialogOptions = {
    autoOpen: false,
    modal: true,
    draggable: false,
    resizable: false,
    appendTo: "#content-root",
    dialogClass: "alert",
    bgiframe: true,
    overlay: {
        backgroundColor: '#fff',
        opacity: 0.5
    },
    // show: { effect: "fade", speed: 250 },
    // hide: { effect: "fade", speed: 5000 },
    hide: 'fold',
    show: 'blind',
    classes: {
        "ui-dialog": "ui-dialog-custom fixed ui-dialog-custom-placement",
    }
}

const jQueryUIdialogDefaultOptions = {
    autoOpen: false,
    modal: true,
    draggable: false,
    resizable: false,
    appendTo: "#content-root",
    dialogClass: "alert",
    bgiframe: true,
    overlay: {
        backgroundColor: '#fff',
        opacity: 0.5
    },
    // show: { effect: "fade", speed: 250 },
    // hide: { effect: "fade", speed: 5000 },
    hide: 'fold',
    show: 'blind',
    classes: {
        "ui-dialog": "",
    }
}

const jQueryUIdialogSmOptions = {
    autoOpen: false,
    modal: true,
    draggable: false,
    resizable: false,
    appendTo: "#content-root",
    dialogClass: "alert",
    bgiframe: true,
    overlay: {
        backgroundColor: '#fff',
        opacity: 0.5
    },
    // show: { effect: "fade", speed: 250 },
    // hide: { effect: "fade", speed: 5000 },
    hide: 'fold',
    show: 'blind',
    classes: {
        "ui-dialog": "ui-dialog-sm fixed ui-dialog-custom-placement",
    }
}

const jQueryUIdialogMdOptions = {
    autoOpen: false,
    modal: true,
    draggable: false,
    resizable: false,
    appendTo: "#content-root",
    dialogClass: "alert",
    bgiframe: true,
    overlay: {
        backgroundColor: '#fff',
        opacity: 0.5
    },
    // show: { effect: "fade", speed: 250 },
    // hide: { effect: "fade", speed: 5000 },
    hide: 'fold',
    show: 'blind',
    classes: {
        "ui-dialog": "ui-dialog-md fixed ui-dialog-custom-placement",
    }
}

const jQueryUIdialogLgOptions = {
    autoOpen: false,
    modal: true,
    draggable: false,
    resizable: false,
    appendTo: "#content-root",
    dialogClass: "alert",
    bgiframe: true,
    overlay: {
        backgroundColor: '#fff',
        opacity: 0.5
    },
    // show: { effect: "fade", speed: 250 },
    // hide: { effect: "fade", speed: 5000 },
    hide: 'fold',
    show: 'blind',
    classes: {
        "ui-dialog": "ui-dialog-lg fixed ui-dialog-custom-placement",
    }
}

const jQueryUIdialogFullOptions = {
    autoOpen: false,
    modal: true,
    draggable: false,
    resizable: false,
    appendTo: "#content-root",
    dialogClass: "alert",
    bgiframe: true,
    overlay: {
        backgroundColor: '#fff',
        opacity: 0.5
    },
    // show: { effect: "fade", speed: 250 },
    // hide: { effect: "fade", speed: 5000 },
    hide: 'fold',
    show: 'blind',
    classes: {
        "ui-dialog": "ui-dialog-full fixed ui-dialog-custom-placement",
    }
}
